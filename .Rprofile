# tell git this is a safe directory inside the docker container
system("git config --global --add safe.directory /home/rstudio/grazing-interaction")

# load environment variables
source("~/grazing-interaction/environment.R")

# load required packages
source("~/grazing-interaction/packages.R")