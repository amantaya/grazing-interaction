# grab all tags from a page
<%*
const theseTags = tp.file.tags;
// separate each tag by a space or by a comma
let tagsString = theseTags.join(" ");
// uncomment this line to remove hashtags
// tagsString = tagsString.replace(/\#/g, '');
tR = tagsString;
%>
